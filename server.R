# server.R

# * unit
# * o = observed counts
# * theta = global proportion - probably can calculate from supplied data
# * n = denominator
# * method = "exact"
# * option = "prop"
# * plot = FALSE

shinyServer(function(input, output, session) {
  
})